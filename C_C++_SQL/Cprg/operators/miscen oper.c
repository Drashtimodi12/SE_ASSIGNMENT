// others operator :*(pointer),&(address of) ,, (comma), sizeof();
#include<stdio.h>
int main(){
    int num;
    float marks;
    long salary;
    double d;
    char ch;
    long double num2;
    printf("\n Size of integer=%d",sizeof(num));
    printf("\n Size of float=%d",sizeof(marks));
    printf("\n Size of long=%d",sizeof(salary));
    printf("\n Size of double=%d",sizeof(d));
    printf("\n Size of char=%d",sizeof(ch));
    printf("\n Size of long double=%d",sizeof(num2));
}